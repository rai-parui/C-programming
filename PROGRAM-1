#include <stdio.h>

int main()
{
    int radius,height;
    printf("\nEnter the radius and height of the cylinder:");
    scanf("%d %d",&radius,&height);
    printf("\nThe volume is:%.2f",3.14*radius*radius*height);

    return 0;
}
